---
phase: 02-translation-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ['src/app/**/*.tsx', 'src/app/en/**/*.tsx']
autonomous: true
---

# Plan 02-01: I18N Folder Routes

## Scope
Duplicazione della struttura app router verso una route `/en` per supportare un approccio linguistico classico, con correzione del mapping statico delle icone bandiera (Italiana verso `/`, Inglese verso `/en`).

## Tasks

<task>
  <action>
    - Utilizzare Node per regex e string replace su tutti i file iterando `href="https://www.archiviomastrosculture.it"` portandoli a `/`.
    - Clona l'intero contenuto della UI statico nella directory `src/app/en/` in modo da esporre path del tipo `/en/biografia` esatti all'utente.
    - Ripulire i collegamenti di bandiera nel nuovo layout inglese cosicché se si ripreme l'italiano, riporti a root `/`.
  </action>
  <verify>
    - `npm run build` deve chiudersi senza conflitti ed includere la route `/en/`.
  </verify>
  <done>
    Struttura di base creata, ora utente può aggiornare l'-HTML per avere una traduzione genuina inglese.
  </done>
</task>